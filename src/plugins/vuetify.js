import Vue from "vue";
import Vuetify, { VToolbar } from "vuetify/lib";

Vue.use(Vuetify, {
  components: {
    VToolbar,
  },
  directives: {
    
  },
});

const opts = {};

export default new Vuetify(opts);
